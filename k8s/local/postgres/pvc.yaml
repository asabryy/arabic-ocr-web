# Local dev only
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: textara
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
